#include "main.h"

/**
 * _supper - checks if a character is uppercase
 * @c: carrier integer Variable
 * Return: 1 if true, 0 if false
 */
int _supper(int c)
{
	if (c >= '65' && c <= '90)'
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
