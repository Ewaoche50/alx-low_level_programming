#include "main.h"

/**
 * _supper - checks if a character is uppercase
 * @c: Variable text
 * Return: Alwayss 0
 */
int _supper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
